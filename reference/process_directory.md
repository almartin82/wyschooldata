# Process raw directory data to standard schema

Takes raw directory data and standardizes column names, types, and adds
derived columns.

## Usage

``` r
process_directory(raw_data)
```

## Arguments

- raw_data:

  Raw data frame from get_raw_directory()

## Value

Processed tibble with standard schema
