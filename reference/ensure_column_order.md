# Ensure consistent column order

Reorders columns to a standard order for consistency.

## Usage

``` r
ensure_column_order(df)
```

## Arguments

- df:

  Data frame to reorder

## Value

Data frame with standard column order
