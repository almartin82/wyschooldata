# Write directory data to cache

Write directory data to cache

## Usage

``` r
write_cache_directory(data, cache_type)
```

## Arguments

- data:

  Data frame to cache

- cache_type:

  Type of cache ("directory_tidy" or "directory_raw")

## Value

Invisibly returns the cache path
