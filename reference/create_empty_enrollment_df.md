# Create empty enrollment data frame

Creates an empty data frame with the expected enrollment schema.

## Usage

``` r
create_empty_enrollment_df()
```

## Value

Empty data frame with enrollment columns
