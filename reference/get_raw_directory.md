# Get raw school directory data from WDE

Downloads and parses two WDE PDFs to assemble directory data: the
accredited schools PDF and the school districts PDF.

## Usage

``` r
get_raw_directory()
```

## Value

Raw data frame with district and school information
